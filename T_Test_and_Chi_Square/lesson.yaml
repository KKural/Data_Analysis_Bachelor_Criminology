# ========== 1. META BLOCK ==========
- Class: meta
  Course: Data Analysis - Bachelor Criminology
  Lesson: T Test and Chi_Square
  Author: Kural K.
  Type: Standard
  Organization: UGent
  Version: 2.5

# ========== 2. WELCOME MESSAGE ==========
- Class: text
  Output: |
    Welcome to the T-Test and Chi-Square lesson!  
    In this module, you'll perform inferential tests to compare groups and examine associations.

# ========== 3. QUESTIONS ==========
- Class: cmd_question
  Output: Q1 Filter the fear_of_crime_survey dataset to include only 'Male' and 'Female' gender values and save it as filtered_survey.
  CorrectAnswer: filtered_survey <- fear_of_crime_survey[fear_of_crime_survey$gender %in% c("Male", "Female"), ]
  AnswerTests: val_matches("filtered_survey <- fear_of_crime_survey[fear_of_crime_survey$gender %in% c(\"Male\", \"Female\"), ]")
  Hint: |
    Type exactly: filtered_survey <- fear_of_crime_survey[fear_of_crime_survey$gender %in% c("Male", "Female"), ]

- Class: cmd_question
  Output: Q2 Perform a t-test to compare feel_safe_day between Males and Females using the filtered_survey dataset.
  CorrectAnswer: t.test(feel_safe_day ~ gender, data = filtered_survey)
  AnswerTests: val_matches("t.test(feel_safe_day ~ gender, data = filtered_survey)")
  Hint: |
    Type exactly: t.test(feel_safe_day ~ gender, data = filtered_survey)

- Class: cmd_question
  Output: Q3 Extract the p-value from the t-test result and save it as p_value.
  CorrectAnswer: p_value <- t.test(feel_safe_day ~ gender, data = filtered_survey)$p.value
  AnswerTests: val_matches("p_value <- t.test(feel_safe_day ~ gender, data = filtered_survey)$p.value")
  Hint: |
    Type exactly: p_value <- t.test(feel_safe_day ~ gender, data = filtered_survey)$p.value
    
- Class: cmd_question
  Output: Q4 Create a contingency table of district × crime_type in crime_stats_ghent and run a chi-square test.
  CorrectAnswer: chisq.test(table(crime_stats_ghent$district, crime_stats_ghent$crime_type))
  AnswerTests: val_matches("chisq.test(table(crime_stats_ghent$district, crime_stats_ghent$crime_type))")
  Hint: |
    Type exactly: chisq.test(table(crime_stats_ghent$district, crime_stats_ghent$crime_type))

- Class: cmd_question
  Output: Q5 Assign the chi-square test to chisq_result and extract its p-value.
  CorrectAnswer: chisq_result <- chisq.test(table(crime_stats_ghent$district, crime_stats_ghent$crime_type)); chisq_result$p.value
  AnswerTests: val_matches("chisq_result <- chisq.test(table(crime_stats_ghent$district, crime_stats_ghent$crime_type)); chisq_result$p.value")
  Hint: |
    Type exactly: chisq_result <- chisq.test(table(crime_stats_ghent$district, crime_stats_ghent$crime_type)); chisq_result$p.value

# ========== 4. COMPLETION MESSAGE ==========
- Class: text
  Output: |
    🎉 You've completed the T-Test & Chi-Square lesson!  
    You can now compare means and test associations in R.
